# TODO

## 좌석 B 좌표 조정
- 현재 영역 `(3, 240, 207, 568)` — 화면 왼쪽 끝에 붙어 있어 가방 박스 중심이
  영역 밖으로 빠지는 경우가 잦다.
- `DEBUG_DRAW_DETECTIONS=True`로 화면을 보면서 실제 좌석 위치에 맞게 재조정.

## 가방 클래스 인식률 (페이즈 4에서 원인 규명, 일단 해결)
- 페이즈 2 검증 중 영상 내내 `bag=0` 관측 → 페이즈 4 진단으로 원인 확인:
  YOLO 탐지는 정상이었고, `PERSON_OVERLAP_THRESH=0.5`가 너무 엄격해
  카페 환경의 정상 가방까지 "사람과 겹침"으로 제외시키고 있었음.
- 임시 조치: `PERSON_OVERLAP_THRESH = 0.85`로 완화. 페이즈 5에서 새 영상으로
  추적 안정성과 함께 재검증 필요.

## `_restore_posted_ids()` 메인 초기화 시 호출 추가
- `board.py:20`에 정의된 함수가 어디서도 호출되지 않음.
- 파이프라인 재시작 시 기존 게시글의 `_posted_ids`가 비어 있어
  중복 등록 / 주인 복귀 시 삭제 누락 발생.
- `main_pipeline.main()` 초기화 블록에 `_restore_posted_ids()` 호출 추가.

## 페이즈 4 SUSPICIOUS 트리거 시각 검증 (페이즈 5 이후)
- 페이즈 4에서 코드 로직 및 부속 동작은 검증 완료. 단, 현재 `case1.mp4`는
  사람이 자리에서 충분히 떠나는 시나리오가 없어 `💺 보류` / `⏱️ SUSPICIOUS`
  진입 로그를 실제 트리거된 상태로 보지 못함.
- 임시 임계값(T_TRACKING=3 등) 단축 테스트에서도 영상 시나리오상 조건 미충족.
- 페이즈 5(BoxMOT 도입) 이후 새 시나리오 영상으로 재시도 — 실제 자리 이탈 →
  10초 경과 → 좌석 점유 상태별 분기 로그를 시각적으로 확인해야 한다.

## 행인 가방 오탐 가능성 (PERSON_OVERLAP_THRESH 부작용)
- `PERSON_OVERLAP_THRESH = 0.85`로 완화하면서, 사람이 들고 있는 가방을
  방치된 가방으로 잘못 잡을 가능성이 커짐 (예전 0.5 설정의 원래 이유).
- 페이즈 5 BoxMOT 도입 시 ReID(외형 기반 재식별)로 자연스럽게 개선 기대 —
  사람과 함께 이동하는 가방은 추적 단계에서 연관 ID가 유지되므로,
  "사람이 자리에 머문 시간" 누적이 정상 작동해 오탐 빈도가 줄어들 전망.
- 페이즈 5 검증 시 행인 가방 오탐 빈도를 별도 카운트하여 추세 비교.
