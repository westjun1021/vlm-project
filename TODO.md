# TODO

## 좌석 B 좌표 조정
- 현재 영역 `(3, 240, 207, 568)` — 화면 왼쪽 끝에 붙어 있어 가방 박스 중심이
  영역 밖으로 빠지는 경우가 잦다.
- `DEBUG_DRAW_DETECTIONS=True`로 화면을 보면서 실제 좌석 위치에 맞게 재조정.

## 가방 클래스 인식률 점검
- 페이즈 2 검증 중 영상 10분 내내 `global: bag=0` 관측.
- 후보 원인:
  - YOLO_CONF=0.45가 너무 높음 (테스트 시 0.35로도 가방이 안 잡혔는지 재확인)
  - `BAG_CLASSES=[24, 26, 28]` 중 실제 영상의 가방이 다른 클래스로 잡힐 가능성
    (e.g., 책가방을 17(개)·73(책)·다른 객체로 오탐)
  - `MIN_BOX_AREA=1500`이 화면 비율 대비 큰 가능성
- 페이즈 3·4 진행과는 별도로, YOLO raw 출력 로깅을 일시적으로 추가하여
  어떤 클래스가 어떤 confidence로 잡히는지 점검 필요.

## `_restore_posted_ids()` 메인 초기화 시 호출 추가
- `board.py:20`에 정의된 함수가 어디서도 호출되지 않음.
- 파이프라인 재시작 시 기존 게시글의 `_posted_ids`가 비어 있어
  중복 등록 / 주인 복귀 시 삭제 누락 발생.
- `main_pipeline.main()` 초기화 블록에 `_restore_posted_ids()` 호출 추가.
